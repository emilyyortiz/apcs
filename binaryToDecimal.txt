0. The base represents the number of symbols in the number system. For example, base-2 has two symbols, 0 and 1. In base-10 the rightmost digit, before a decimal point, has the value of x * 10^0. The next digit on the left has a value of x * 10^1, and so on.

1. Binary is a base-2 number system. It is convenient in computing because each digit can have two states, "on" or "off."

2. Binary To Decimal Algorithm
 
 n is the input
 set j = n
 sum = 0
 while j > 0, 
